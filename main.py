'''

list.append(x), list.sort([key=функция]), list.count(x), list.index(x, [start [, end]]), list.remove(x)

dict.get(key[, default]), dict.items(), dict.keys(), dict.values(), dict.update([other])

set.add(elem), set.update(other, ...), set.intersection_update(other, ...), set.difference_update(other, ...), set.remove(elem)

S.find(str, [start],[end]), len(S), S.split(символ), S.count(str, [start],[end]), S.replace(шаблон, замена[, maxcount])

'''